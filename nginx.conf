events{}

http {
    server {
           listen 80;

           location / {
              proxy_pass http://dockerimagepractice-app-1:8082;
              proxy_set_header Host $host;
              proxy_set_header X-Real-IP $remote>addr;
              }
        }
}